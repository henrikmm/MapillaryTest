# Pin only the things this experiment actually imports.
# Note: torch / transformers / cv2 are expected to already be installed in the
# environment shared with DINOEXPERIMENT.
pyyaml>=6.0
numpy>=1.24
scipy>=1.10
pyproj>=3.6           # WGS84 -> ENU conversions for stage 0 / 1
pillow>=10.0
tqdm>=4.66
matplotlib>=3.7       # diagnostics overlays
# Stage 2/4 segmentation (Cityscapes SegFormer); see ASSUMPTIONS.md A6.
transformers>=4.40
# OpenSfM itself is NOT a pip dep — install separately (Docker recommended).
